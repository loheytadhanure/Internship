

  {
  "version": 2,
  "builds": [
    {
      "src": "ecommerce/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "ecommerce/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
